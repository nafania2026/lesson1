# lesson1
Создан файл lesson_1_task_1.py.
В файле создана переменная my_name.
Присвоена переменной значение — ваше имя. Помните, что строковые значения указываются в кавычках.
Написана команда вывода значения переменной на экран. Использована функция print.

Перезаписана переменная
Создан файл lesson_1_task_2.py.
В файле создана переменная my_age и присвоено ей значение — мой возраст.
В следующей строке присвоено этой же переменной другое значение — мой возраст через три года.
Написана команда вывода значения переменной на экран. Использована функция print.
Запущен скрипт.

Получен пользовательский ввод
Создан файл lesson_1_task_3.py.
Создана переменная first_name.
Создана переменная last_name.
Задано значение обеих переменных через функцию input.
Выведен на экран текст в формате «Вас зовут: last_name first_name».

Создание функции
Создан файл lesson_1_task_4.py.
Объявлена функция print_greeting().
Тело функции: печать на экран выражения «Привет, мир!».
Напиисан код, который вызывает функцию print_greeting().
Объявлена функция (def) должна быть в коде до ее вызова.

Параметризация функций
Создан файл lesson_1_task_5.py.
Создана функцию, которая принимает на вход параметр num и печатает этот параметр в консоль.
Вызвана эта функция 11 раз так, чтобы в консоль вывелся номер 88005553535.

